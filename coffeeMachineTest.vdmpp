class CMTest
operations

public Execute: () ==> ()
Execute() ==
	let ts : TestSuite = new TestSuite(),
 	Testresult = new TestResult()
	in
	 (ts.addTest(new CupControllerTest());
		ts.run(Testresult);
		IO`println(Testresult.toString()););
end CMTest