class World is subclass of GLOBAL

instance variables

static public env : Environment := new Environment("scenario.txt", "output.txt");
static public timerRef : Timer := new Timer();
  
operations

public Run: () ==> ()
Run() == (env.Run(); env.ShowResult());
end World