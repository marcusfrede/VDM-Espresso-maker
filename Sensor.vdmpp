class Sensor

types
public sensorType 	= <TEMPSENSOR> | <PRESSURESENSOR> | <NONE>;

instance variables

protected ID	: nat;
protected Type : sensorType;
protected Value	: nat;
protected Env	: Environment;

operations

public GetID: () ==> nat
	GetID() ==
		return ID;
		
public GetType: () ==> sensorType
	GetType() ==
		return Type;

pure public ReadValue: () ==> nat
	ReadValue() ==
		return Value;

public Step: () ==> ()
	Step() ==
		is subclass responsibility

end Sensor