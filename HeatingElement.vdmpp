class HeatingElement

instance variables

protected ID	: nat;

operations

public HeatingElement: nat ==> HeatingElement
	HeatingElement (id) ==
		ID := id;

public GetID: () ==> nat
	GetID() ==
		return ID;

public Step: () ==> ()
	Step() == (
		let ot = World`env.ReadTemp(),
		    nt = World`env.IncTemp()
		in    	
		if World`env.verbose then	IO`printf("\nIncremented temperature from %1s to %2s", [ot,nt]);		
)
--post World`env.ReadTemp() = World`env.ReadTemp() + 1

end HeatingElement